## 2024-01-24 - Notification Accessibility Pattern
**Learning:** Adding dynamic unread counts to `aria-label` (e.g., "Notifications, 5 unread") significantly improves context for screen reader users compared to static labels like "Notifications".
**Action:** Apply this pattern to other badge-like components (Messages, Mentions) where count conveys urgency or status.
