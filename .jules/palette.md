## 2024-10-24 - Interactive Input Icons
**Learning:** The `Input` component was modified to support interactive right icons (e.g., clear button) by conditionally rendering a button instead of a div and managing pointer-events.
**Action:** Use the `onRightIconClick` and `rightIconAriaLabel` props on `Input` for any future input fields requiring internal actions.
