/**
 * Storage Constants
 * Shared constants for sessionStorage and localStorage keys
 */

/**
 * Key used in sessionStorage to store toast messages that should be displayed
 * after a page redirect (e.g., after account deletion)
 */
export const TOAST_ON_LOAD_KEY = 'toastOnLoad'
